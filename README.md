libds4: simple C library to read input and send output to DualShock 4 controllers using hidapi.



To build and test a sample(**You must have vcpkg installed and "VCPKG_ROOT" set to install location**):
```bash
git clone https://github.com/Tm24sense/libds4.git
cd libds4
cmake --preset build

```

